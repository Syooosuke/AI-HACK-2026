.DS_Store

# Claude Code が作る作業用ディレクトリ（git worktree 等）。リポジトリには含めない
# ディレクトリ自体を除外すると中を再包含できないため、中身を対象にする
.claude/*
# Issue の書き方はチーム共通のルールなので共有する
!.claude/issue-guidelines.md

# デプロイ設定（秘密情報を含む）
spotcheck-ai/deploy/env.sh
